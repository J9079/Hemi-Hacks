{
  "name": "surplus-to-shelter",
  "version": "1.0.0",
  "description": "Real-Time Food Rescue Routing Platform - Production Unified Deployment",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install --include=dev",
    "build:client": "cd client && npx vite build",
    "build": "npm run install:all && npm run build:client",
    "start": "node server/server.js",
    "seed": "node server/seeds/seedData.js",
    "test:e2e": "node server/test/e2e.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1",
    "socket.io": "^4.7.5"
  }
}
